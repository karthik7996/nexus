pipeline {
    agent any
    stages {
        stage('docker-build') {
            steps {
               echo "creating the container" 
             //sh 'docker run -d --name k1 centos:latest'    
            }
        }
    }
}
